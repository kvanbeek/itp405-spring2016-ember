<div class="container">
  <nav class="navbar navbar-light bg-faded">
    <ul class="nav navbar-nav">
      <li class="nav-item active">
        <!-- <aclass="nav-link" href="#">Home </a> -->
        {{#link-to 'index'}}
          <div class="nav-link">
            Home <span class="sr-only">(current)</span>
          </div>
        {{/link-to}}
      </li>
      {{#if user.loggedIn}}
        <li class="nav-item">
          {{#link-to 'admin'}}
            <div class="nav-link">
              Admin<span class="sr-only">(current)</span>
            </div>
          {{/link-to}}
        </li>
       {{/if}}
       <li class="nav-item pull-xs-right">
         {{#if user.loggedIn}}
            <a><div class="nav-link" {{action "logout" on="click"}}>Logout</div></a>
          {{else}}
            {{#link-to "login"}}<div class="nav-link">Login</div>{{/link-to}}  
         {{/if}}
        
      </li>
      <li class="nav-item pull-xs-right">
        {{#link-to "shoppingcart"}}<div class="nav-link"><i class="fa fa-shopping-cart" aria-hidden="true"></i> {{shoppingCart.items.length}}</div>{{/link-to}}
      </li>

    </ul>
  </nav>
    <div class="row">
        {{outlet}}
    </div>
</div>
